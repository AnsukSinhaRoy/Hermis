experiment:
  name: "sharpe_weekly_k25_both"  #  choose a name for your experiment
  #tag: "mvreg"
  start_date: "2015-11-06" #yyyy-mm-dd
  end_date: "2025-03-05"
  rebalance: "weekly"  #  choose one: daily | weekly | monthly
  seed: 42
  use_gpu: false

data:
  mode: "processed"  # 👈 choose one: synthetic | processed
  # Option A (recommended): pick which processed dataset to use
  processed:
    dataset: "both"   # 👈 choose one: india | us | both
    base_dir: "data/processed"
    freq: "1D"

risk_model:
  type: "sample_cov"
  lookback: 252

csv_path: ""

optimizer:
  type: sharpe
  box: {min: 0.0, max: 0.20}
  long_only: true


transaction_costs:
  proportional: 0.0005
