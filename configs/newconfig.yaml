experiment:
  name: "campaign_daily"  # 👈 choose a name for your experiment
  #tag: "mvreg"
  start_date: "2015-06-06" #yyyy-mm-dd
  end_date: "2020-02-12"
  rebalance: "daily"  # 👈 choose one: daily | weekly | monthly
  seed: 42
  use_gpu: false

data:
  mode: "processed"  # 👈 choose one: synthetic | processed
  processed_path: "data/processed/prices_1D.parquet"

risk_model:
  type: "sample_cov"
  lookback: 252

csv_path: ""

optimizer:
  type: "risk_parity"         # 👈 choose one: mv_reg | risk_parity | minvar
  k_cardinality: 25
  # lambdas: [0.001, 0.01, 0.1, 1.0]
  box:
    min: 0.001
    max: 1.00
  long_only: true

transaction_costs:
  proportional: 0.0005
